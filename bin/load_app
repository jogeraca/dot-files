#!/usr/bin/env bash

program=$1
class=$2
param=$3

if ps -C "$program" >/dev/null; then
    swaymsg "[class=\"$class\"] focus"
else
    echo $program >> /tmp/dataaa
    echo $class>> /tmp/dataaa
    echo $param>> /tmp/dataaa
    swaymsg "exec $param $program"
fi
